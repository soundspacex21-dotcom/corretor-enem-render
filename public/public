<!DOCTYPE html>
<html lang="pt-BR">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0" />
<title>Corretor de Redação — ENEM</title>
<link href="https://fonts.googleapis.com/css2?family=Syne:wght@400;500;600;700;800&family=DM+Serif+Display:ital@0;1&family=DM+Sans:wght@400;500&display=swap" rel="stylesheet" />
<style>
*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
:root {
  --bg: #0F0F0D; --surface: #1A1A17; --surface2: #242420;
  --text: #F0EDE4; --muted: #888880; --border: #2E2E28;
  --accent: #E8D44D; --green: #4CAF7D; --green-bg: #0D2018;
  --amber: #F0A030; --amber-bg: #221500; --red: #E05050; --red-bg: #200A0A;
  --radius: 14px; --radius-sm: 8px;
}
body { font-family: 'DM Sans', sans-serif; background: var(--bg); color: var(--text); min-height: 100vh; }

header {
  padding: 1.25rem 2rem; display: flex; align-items: center; gap: 14px;
  border-bottom: 1px solid var(--border); position: sticky; top: 0;
  background: rgba(15,15,13,0.92); backdrop-filter: blur(12px); z-index: 100;
}
.logo { font-family: 'Syne', sans-serif; font-weight: 800; font-size: 15px; color: var(--accent); }
.logo span { color: var(--text); opacity: 0.4; font-weight: 400; margin-left: 8px; font-size: 13px; }

.hero { padding: 4rem 2rem 2rem; max-width: 860px; margin: 0 auto; }
.hero-eyebrow {
  display: inline-flex; align-items: center; gap: 6px;
  font-size: 11px; font-weight: 500; letter-spacing: 1.5px; text-transform: uppercase;
  color: var(--accent); margin-bottom: 1.25rem;
}
.hero-eyebrow::before { content: ''; width: 20px; height: 1px; background: var(--accent); }
.hero h1 { font-family: 'DM Serif Display', serif; font-size: clamp(34px,6vw,54px); line-height: 1.05; letter-spacing: -1px; margin-bottom: 1rem; }
.hero h1 em { font-style: italic; color: var(--accent); }
.hero p { font-size: 16px; color: var(--muted); line-height: 1.7; max-width: 520px; }

.comp-strip { display: flex; gap: 6px; flex-wrap: wrap; margin: 1.5rem auto; max-width: 860px; padding: 0 2rem; }
.comp-tag { font-size: 11px; font-weight: 500; padding: 5px 12px; border-radius: 99px; border: 1px solid var(--border); color: var(--muted); }

.main { max-width: 860px; margin: 0 auto; padding: 0 2rem 5rem; }

.tabs { display: flex; gap: 2px; background: var(--surface); border: 1px solid var(--border); border-radius: var(--radius); padding: 4px; margin-bottom: 1.25rem; }
.tab { flex: 1; padding: 10px; border: none; border-radius: 10px; background: transparent; color: var(--muted); font-family: 'DM Sans', sans-serif; font-size: 14px; font-weight: 500; cursor: pointer; transition: all 0.18s; display: flex; align-items: center; justify-content: center; gap: 7px; }
.tab svg { opacity: 0.6; }
.tab.active { background: var(--surface2); color: var(--text); }
.tab.active svg { opacity: 1; }
.tab:hover:not(.active) { color: var(--text); }

.panel { display: none; }
.panel.active { display: block; }

.card { background: var(--surface); border: 1px solid var(--border); border-radius: var(--radius); padding: 1.5rem; margin-bottom: 1rem; }
.field-label { display: block; font-size: 11px; font-weight: 500; letter-spacing: 1px; text-transform: uppercase; color: var(--muted); margin-bottom: 10px; }

input[type=text] { width: 100%; padding: 11px 14px; border: 1px solid var(--border); border-radius: var(--radius-sm); font-size: 15px; font-family: 'DM Sans', sans-serif; color: var(--text); background: var(--bg); transition: border-color 0.15s; }
input[type=text]:focus { outline: none; border-color: var(--accent); }
input[type=text]::placeholder { color: var(--muted); opacity: 0.6; }

textarea { width: 100%; min-height: 280px; padding: 14px; border: 1px solid var(--border); border-radius: var(--radius-sm); font-size: 15px; font-family: 'DM Sans', sans-serif; color: var(--text); background: var(--bg); resize: vertical; line-height: 1.8; transition: border-color 0.15s; }
textarea:focus { outline: none; border-color: var(--accent); }
textarea::placeholder { color: var(--muted); opacity: 0.5; }
.meta-row { display: flex; justify-content: space-between; margin-top: 8px; font-size: 12px; color: var(--muted); }

.upload-zone { border: 2px dashed var(--border); border-radius: var(--radius); padding: 3rem 2rem; text-align: center; cursor: pointer; transition: all 0.2s; background: var(--surface); position: relative; }
.upload-zone:hover, .upload-zone.dragover { border-color: var(--accent); background: rgba(232,212,77,0.04); }
.upload-zone input[type=file] { position: absolute; inset: 0; opacity: 0; cursor: pointer; width: 100%; height: 100%; }
.upload-icon { width: 52px; height: 52px; border-radius: 12px; background: var(--surface2); border: 1px solid var(--border); display: flex; align-items: center; justify-content: center; margin: 0 auto 1rem; }
.upload-zone h3 { font-family: 'Syne', sans-serif; font-size: 16px; font-weight: 600; color: var(--text); margin-bottom: 6px; }
.upload-zone p { font-size: 13px; color: var(--muted); line-height: 1.5; }
.upload-zone .hint { display: inline-block; margin-top: 12px; font-size: 11px; padding: 4px 10px; border-radius: 99px; border: 1px solid var(--border); color: var(--muted); }

.preview-wrap { position: relative; border-radius: var(--radius); overflow: hidden; background: var(--surface); border: 1px solid var(--border); margin-bottom: 1rem; display: none; }
.preview-wrap img { width: 100%; max-height: 420px; object-fit: contain; display: block; background: #000; }
.preview-bar { padding: 10px 14px; display: flex; align-items: center; justify-content: space-between; border-top: 1px solid var(--border); }
.preview-bar span { font-size: 13px; color: var(--muted); }
.btn-remove { font-size: 12px; color: var(--red); background: none; border: none; cursor: pointer; font-family: 'DM Sans', sans-serif; padding: 4px 8px; border-radius: 6px; transition: background 0.15s; }
.btn-remove:hover { background: var(--red-bg); }

.btn-submit { width: 100%; padding: 15px; background: var(--accent); color: #0F0F0D; border: none; border-radius: var(--radius-sm); font-size: 16px; font-family: 'Syne', sans-serif; font-weight: 700; letter-spacing: -0.2px; cursor: pointer; transition: opacity 0.15s, transform 0.1s; }
.btn-submit:hover { opacity: 0.88; }
.btn-submit:active { transform: scale(0.99); }
.btn-submit:disabled { opacity: 0.3; cursor: not-allowed; transform: none; }

.error-msg { text-align: center; color: var(--red); font-size: 14px; margin-top: 0.75rem; display: none; }

#result-area { display: none; margin-top: 2.5rem; }
.result-top { display: flex; align-items: flex-end; justify-content: space-between; margin-bottom: 1.5rem; gap: 16px; }
.result-top h2 { font-family: 'DM Serif Display', serif; font-size: 26px; color: var(--text); }
.total-num { font-family: 'Syne', sans-serif; font-size: 42px; font-weight: 800; color: var(--accent); line-height: 1; }
.total-sub { font-size: 12px; color: var(--muted); margin-top: 2px; text-align: right; }

.score-bars { background: var(--surface); border: 1px solid var(--border); border-radius: var(--radius); padding: 1.25rem 1.5rem; margin-bottom: 1rem; display: grid; gap: 14px; }
.score-row { display: grid; grid-template-columns: 90px 1fr 50px; align-items: center; gap: 12px; }
.score-row-label { font-size: 12px; font-weight: 500; color: var(--muted); }
.bar-track { height: 6px; background: var(--surface2); border-radius: 99px; overflow: hidden; }
.bar-fill { height: 100%; border-radius: 99px; transition: width 0.8s cubic-bezier(.16,1,.3,1); }
.bar-fill.alta { background: var(--green); }
.bar-fill.media { background: var(--amber); }
.bar-fill.baixa { background: var(--red); }
.score-row-num { font-family: 'Syne', sans-serif; font-size: 15px; font-weight: 700; text-align: right; }
.score-row-num.alta { color: var(--green); }
.score-row-num.media { color: var(--amber); }
.score-row-num.baixa { color: var(--red); }

.feedback-list { display: flex; flex-direction: column; gap: 8px; margin-top: 1rem; }
.fb-card { border-radius: var(--radius); border: 1px solid var(--border); overflow: hidden; background: var(--surface); }
.fb-header { padding: 12px 16px; display: flex; align-items: center; justify-content: space-between; gap: 12px; border-bottom: 1px solid var(--border); }
.fb-title { font-size: 13px; font-weight: 500; color: var(--text); flex: 1; }
.fb-badge { font-size: 11px; font-weight: 600; padding: 3px 10px; border-radius: 99px; white-space: nowrap; letter-spacing: 0.3px; }
.fb-badge.alta { background: var(--green-bg); color: var(--green); border: 1px solid rgba(76,175,125,0.2); }
.fb-badge.media { background: var(--amber-bg); color: var(--amber); border: 1px solid rgba(240,160,48,0.2); }
.fb-badge.baixa { background: var(--red-bg); color: var(--red); border: 1px solid rgba(224,80,80,0.2); }
.fb-body { padding: 14px 16px; font-size: 14px; line-height: 1.75; color: rgba(240,237,228,0.75); }

.loading-state { padding: 3rem; text-align: center; }
.spinner { width: 36px; height: 36px; border: 2px solid var(--border); border-top-color: var(--accent); border-radius: 50%; animation: spin 0.8s linear infinite; margin: 0 auto 1rem; }
@keyframes spin { to { transform: rotate(360deg); } }
.loading-state p { font-size: 14px; color: var(--muted); }

.footer { text-align: center; font-size: 12px; color: var(--muted); opacity: 0.5; margin-top: 3rem; padding-top: 1.5rem; border-top: 1px solid var(--border); }

@media (max-width: 600px) {
  .hero, .main { padding-left: 1.25rem; padding-right: 1.25rem; }
  .comp-strip { padding: 0 1.25rem; }
  .score-row { grid-template-columns: 70px 1fr 44px; }
  header { padding: 1rem 1.25rem; }
}
</style>
</head>
<body>

<header>
  <div class="logo">ENEM<span>corretor de redação · IA</span></div>
</header>

<div class="hero">
  <div class="hero-eyebrow">Powered by Inteligência Artificial</div>
  <h1>Sua redação corrigida<br>em <em>segundos</em></h1>
  <p>Envie o texto digitado ou uma foto da sua redação manuscrita. A IA avalia nas 5 competências do ENEM com feedback detalhado.</p>
</div>

<div class="comp-strip">
  <span class="comp-tag">C1 · Língua portuguesa</span>
  <span class="comp-tag">C2 · Tema e repertório</span>
  <span class="comp-tag">C3 · Organização</span>
  <span class="comp-tag">C4 · Coesão</span>
  <span class="comp-tag">C5 · Intervenção social</span>
</div>

<div class="main">
  <div class="card">
    <label class="field-label" for="theme-input">Tema da redação</label>
    <input type="text" id="theme-input" placeholder="Ex: Desafios da inclusão digital no Brasil contemporâneo" />
  </div>

  <div class="tabs">
    <button class="tab active" onclick="switchTab('text', this)">
      <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><polyline points="14,2 14,8 20,8"/><line x1="16" y1="13" x2="8" y2="13"/><line x1="16" y1="17" x2="8" y2="17"/></svg>
      Digitar texto
    </button>
    <button class="tab" onclick="switchTab('photo', this)">
      <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="3" width="18" height="18" rx="2"/><circle cx="8.5" cy="8.5" r="1.5"/><polyline points="21,15 16,10 5,21"/></svg>
      Enviar foto
    </button>
  </div>

  <div class="panel active" id="panel-text">
    <div class="card">
      <label class="field-label" for="essay-input">Texto da redação</label>
      <textarea id="essay-input" placeholder="Cole ou escreva sua redação aqui...&#10;&#10;Introdução → Desenvolvimento (2 parágrafos) → Conclusão com proposta de intervenção."></textarea>
      <div class="meta-row">
        <span id="word-count">0 palavras</span>
        <span>Ideal: 250–350 palavras</span>
      </div>
    </div>
  </div>

  <div class="panel" id="panel-photo">
    <div class="preview-wrap" id="preview-wrap">
      <img id="preview-img" src="" alt="Prévia da redação" />
      <div class="preview-bar">
        <span id="preview-name">imagem.jpg</span>
        <button class="btn-remove" onclick="removePhoto()">✕ Remover</button>
      </div>
    </div>
    <div class="upload-zone" id="upload-zone">
      <input type="file" id="photo-input" accept="image/*" onchange="handlePhoto(this)" />
      <div class="upload-icon">
        <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="#888" stroke-width="1.5"><rect x="3" y="3" width="18" height="18" rx="2"/><circle cx="8.5" cy="8.5" r="1.5"/><polyline points="21,15 16,10 5,21"/></svg>
      </div>
      <h3>Envie a foto da redação</h3>
      <p>Arraste e solte aqui ou clique para selecionar<br>Funciona com redações manuscritas e digitadas</p>
      <span class="hint">JPG · PNG · HEIC · até 20MB</span>
    </div>
    <div class="card" style="margin-top:1rem">
      <label class="field-label" for="photo-note">Observações (opcional)</label>
      <input type="text" id="photo-note" placeholder="Ex: letra cursiva, papel pautado..." />
    </div>
  </div>

  <button class="btn-submit" id="btn-corrigir" onclick="corrigir()">Corrigir redação</button>
  <div class="error-msg" id="error-msg"></div>

  <div id="result-area">
    <div id="result-content"></div>
  </div>

  <div class="footer">
    Avaliação por IA com base na Matriz de Referência oficial do ENEM · INEP
  </div>
</div>

<script>
let currentTab = 'text';
let photoBase64 = null;
let photoMime = null;

function switchTab(tab, btn) {
  currentTab = tab;
  document.querySelectorAll('.tab').forEach(t => t.classList.remove('active'));
  document.querySelectorAll('.panel').forEach(p => p.classList.remove('active'));
  btn.classList.add('active');
  document.getElementById('panel-' + tab).classList.add('active');
}

const textarea = document.getElementById('essay-input');
textarea.addEventListener('input', () => {
  const words = textarea.value.trim() === '' ? 0 : textarea.value.trim().split(/\s+/).length;
  document.getElementById('word-count').textContent = words + ' palavra' + (words !== 1 ? 's' : '');
});

const zone = document.getElementById('upload-zone');
zone.addEventListener('dragover', e => { e.preventDefault(); zone.classList.add('dragover'); });
zone.addEventListener('dragleave', () => zone.classList.remove('dragover'));
zone.addEventListener('drop', e => { e.preventDefault(); zone.classList.remove('dragover'); if (e.dataTransfer.files[0]) processFile(e.dataTransfer.files[0]); });

function handlePhoto(input) { if (input.files && input.files[0]) processFile(input.files[0]); }

function processFile(file) {
  if (!file.type.startsWith('image/')) { showError('Por favor, envie um arquivo de imagem.'); return; }
  const reader = new FileReader();
  reader.onload = (e) => {
    const result = e.target.result;
    photoBase64 = result.split(',')[1];
    photoMime = file.type;
    document.getElementById('preview-img').src = result;
    document.getElementById('preview-name').textContent = file.name;
    document.getElementById('preview-wrap').style.display = 'block';
    document.getElementById('upload-zone').style.display = 'none';
  };
  reader.readAsDataURL(file);
}

function removePhoto() {
  photoBase64 = null; photoMime = null;
  document.getElementById('preview-wrap').style.display = 'none';
  document.getElementById('upload-zone').style.display = 'block';
  document.getElementById('photo-input').value = '';
}

function showError(msg) {
  const el = document.getElementById('error-msg');
  el.textContent = msg; el.style.display = 'block';
}

async function corrigir() {
  const theme = document.getElementById('theme-input').value.trim();
  const btn = document.getElementById('btn-corrigir');
  const resultArea = document.getElementById('result-area');
  const resultContent = document.getElementById('result-content');
  document.getElementById('error-msg').style.display = 'none';

  if (currentTab === 'text' && textarea.value.trim().split(/\s+/).length < 30) {
    showError('Escreva pelo menos 30 palavras para a correção.'); return;
  }
  if (currentTab === 'photo' && !photoBase64) {
    showError('Por favor, envie uma foto da redação.'); return;
  }

  btn.disabled = true; btn.textContent = 'Analisando...';
  resultArea.style.display = 'block';
  resultContent.innerHTML = `<div class="loading-state"><div class="spinner"></div><p>Analisando sua redação nas 5 competências do ENEM...</p></div>`;
  resultArea.scrollIntoView({ behavior: 'smooth', block: 'start' });

  try {
    const payload = {
      mode: currentTab,
      theme,
      essay: currentTab === 'text' ? textarea.value.trim() : null,
      photoBase64: currentTab === 'photo' ? photoBase64 : null,
      photoMime: currentTab === 'photo' ? photoMime : null,
      photoNote: currentTab === 'photo' ? document.getElementById('photo-note').value.trim() : null
    };

    const res = await fetch('/api/corrigir', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(payload)
    });

    const data = await res.json();
    if (data.error) throw new Error(data.error);

    const competencias = data.competencias;
    const total = competencias.reduce((s, c) => s + c.nota, 0);
    const nivelLabel = { alta: 'Bom · ', media: 'Regular · ', baixa: 'Fraco · ' };

    resultContent.innerHTML = `
      <div class="result-top">
        <h2>Resultado da correção</h2>
        <div><div class="total-num">${total}</div><div class="total-sub">de 1000 pontos</div></div>
      </div>
      <div class="score-bars">
        ${competencias.map((c, i) => `
          <div class="score-row">
            <div class="score-row-label">C${i+1}</div>
            <div class="bar-track"><div class="bar-fill ${c.nivel}" style="width:${(c.nota/200)*100}%"></div></div>
            <div class="score-row-num ${c.nivel}">${c.nota}</div>
          </div>
        `).join('')}
      </div>
      <div class="feedback-list">
        ${competencias.map((c, i) => `
          <div class="fb-card">
            <div class="fb-header">
              <span class="fb-title"><strong>C${i+1}</strong> — ${c.desc}</span>
              <span class="fb-badge ${c.nivel}">${nivelLabel[c.nivel]}${c.nota} pts</span>
            </div>
            <div class="fb-body">${c.feedback}</div>
          </div>
        `).join('')}
      </div>`;
  } catch (err) {
    resultContent.innerHTML = '';
    showError('Erro ao processar. Verifique sua conexão e tente novamente.');
  }

  btn.disabled = false; btn.textContent = 'Corrigir redação';
}
</script>
</body>
</html>
